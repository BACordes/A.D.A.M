from GUIModule import *

if __name__ == "__main__":
    # initialize the drivers in another thread
    # initialize the communication apparatus and thread
    # make sure the android app is up and running
    # wait for user input in another thread

    # wait for user input
    # console_interface = ConsoleModule()
    # print("threads:", threading.active_count())
    # console_interface.start()
    ADAMGraphicalInterface()

